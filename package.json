{
  "name": "calls-batch",
  "description": "Execute calls in debounced batches, with pre/postflush hooks, useful for performance.",
  "version": "1.2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc --skipLibCheck && tstei",
    "compile:watch": "tsc --skipLibCheck --watch",
    "prepublishOnly": "npm run clean && npm run compile"
  },
  "bugs": {
    "url": "https://github.com/fabiospampinato/calls-batch/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Fabio Spampinato",
    "email": "spampinabio@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fabiospampinato/calls-batch.git"
  },
  "keywords": [
    "calls",
    "batch",
    "performance"
  ],
  "dependencies": {
    "debounce": "^1.2.0"
  },
  "devDependencies": {
    "@types/debounce": "^1.2.0",
    "@types/node": "^10.9.4",
    "rimraf": "^2.6.0",
    "typescript": "^3.0.3",
    "typescript-transform-export-interop": "^1.0.0"
  }
}
